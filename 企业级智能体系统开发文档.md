企业级智能体系统 — 本地开发项目文档

项目概述
本项目旨在构建一个面向企业场景的本地智能体（Agent）系统，具备工作流编排、工具调用、记忆管理、RAG 知识库四大核心能力。通过标准化 API 层对接 ERP/CRM/OA/钉钉/企微等企业系统，内置容错重试与降级机制，支持 Docker Compose 本地部署及 Kubernetes 集群部署。

技术栈：Python 3.11+ · FastAPI · LangGraph · LangChain · Pydantic v2 · ChromaDB/Milvus · Redis · SQLAlchemy · MCP SDK · Docker · Helm/K8s
设计原则：
  编排层透明可控（LangGraph 状态图）
  工具层自研封装（降低第三方依赖）
  记忆层分层设计（短期会话 + 长期持久）
  知识层混合检索（向量 + 关键词）
  接入层统一抽象（Channel + Skill 模式）

整体架构设计（步骤1）

2.1 分层架构
text
┌─────────────────────────────────────────────────────────┐
│                    接入层 (Channel Layer)               │
│  钉钉 Channel │ 企微 Channel │ OA Channel │ ERP/CRM     │
├─────────────────────────────────────────────────────────┤
│                   编排层 (Orchestration Layer)          │
│  LangGraph StateGraph: Router → Planner → Skill →     │
│                        Tool → Reviewer → END          │
├─────────────────────────────────────────────────────────┤
│                     工具层 (Tool Layer)                 │
│      Native Tool Calling │ MCP Protocol │ HTTP Connector│
├─────────────────────────────────────────────────────────┤
│                    记忆层 (Memory Layer)                │
│  短期记忆: Redis (会话状态/TTL/滚动摘要)                │
│  长期记忆: MySQL/PostgreSQL (事实三元组/偏好/历史)      │
├─────────────────────────────────────────────────────────┤
│                   知识层 (Knowledge Layer)              │
│  向量库: ChromaDB(开发) / Milvus(生产)                 │
│  检索: Dense + BM25 → RRF → Cross-Encoder 重排         │
├─────────────────────────────────────────────────────────┤
│                     LLM 推理层                          │
│  DeepSeek / Qwen / Ollama 本地模型 (OpenAI 兼容接口)   │
└─────────────────────────────────────────────────────────┘

2.2 LangGraph 工作流编排
工作流基于 Router → Planner → Skill → Tool → Reviewer 五阶段链路，通过 Pydantic 结构化输出衔接各阶段。

2.2.1 状态定义
python
from typing import TypedDict, List, Tuple, Any, Optional, Literal

class AgentState(TypedDict, total=False):
    user_input: str                                    # 用户输入
    session_id: str                                    # 会话标识
    route: Literal["knowledge", "data", "mixed"]       # 任务路由结果
    chat_history: List[dict]                           # 对话历史（OpenAI 消息格式）
    intermediate_steps: List[Tuple[str, str, Any]]     # 执行轨迹
    knowledge_evidence: List[dict]                     # 检索到的知识证据
    tool_results: List[dict]                           # 工具调用结果
    final_output: Optional[str]                        # 最终输出
    retry_count: int                                   # 重试计数
    step_number: int                                   # 当前步骤编号
    errors: List[str]                                  # 错误记录

2.2.2 图结构构建
python
from langgraph.graph import StateGraph, END
from langgraph.prebuilt import ToolExecutor

class AgentWorkflow:
    def init(self, llm, tools: list, memory_manager, retriever):
        self.llm = llm
        self.tools = tools
        self.tool_executor = ToolExecutor(tools)
        self.memory = memory_manager
        self.retriever = retriever
        self.graph = self._build_graph()

    def _build_graph(self) -> StateGraph:
        graph = StateGraph(AgentState)
        
        # 添加节点
        graph.add_node("router", self._router_node)
        graph.add_node("planner", self._planner_node)
        graph.add_node("retrieve", self._retrieve_node)
        graph.add_node("tool_call", self._tool_call_node)
        graph.add_node("reviewer", self._reviewer_node)
        graph.add_node("respond", self._respond_node)

        # 入口与路由分支
        graph.set_entry_point("router")
        graph.add_conditional_edges("router", self._route_decision, {
            "knowledge": "retrieve", "data": "planner", "mixed": "retrieve"
        })
        
        # 边连接
        graph.add_edge("retrieve", "planner")
        graph.add_conditional_edges("planner", self._should_call_tool, {
            "tool": "tool_call", "respond": "reviewer"
        })
        graph.add_edge("tool_call", "planner") # 多步工具调用循环
        graph.add_edge("reviewer", "respond")
        graph.add_edge("respond", END)

        return graph.compile()

2.3 记忆管理设计
采用双视野记忆模型：
短期记忆（Redis）：管理会话状态（TTL 30分钟），包含滚动摘要与上下文预算控制。
长期记忆（PostgreSQL）：将对话中提取的事实以三元组形式持久化，支持基于访问频率的自动提升和基于时间的修剪。

python
class LongTermMemoryStore:
    """长期记忆存储 — 事实三元组 + 用户偏好"""
    async def extract_and_store(self, session_id: str, dialogue: list[dict]):
        """从对话中提取原子事实并持久化"""
        facts = await self._extract_facts(dialogue)
        for fact in facts:
            existing = await self._find_conflict(fact) # 去重与冲突检测
            if existing and fact["confidence"] > existing["confidence"]:
                await self._update_fact(existing["id"], fact)
            else:
                await self._insert_fact(session_id, fact)

RAG 知识库搭建（步骤2）

3.1 文档加载与智能切分
优先按语义边界（段落 → 换行 → 句子）分割，保留上下文连贯性。
python
from langchain.text_splitter import RecursiveCharacterTextSplitter

def split_documents(documents, chunk_size: int = 500, chunk_overlap: int = 50):
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=chunk_size,
        chunk_overlap=chunk_overlap,
        separators=["nn", "n", "。", "！", "？", ".", " ", ""],
    )
    return splitter.split_documents(documents)

3.2 向量化与混合检索
采用 Dense（向量）+ BM25（关键词）→ RRF 融合 的混合检索链路。
python
import chromadb
from rank_bm25 import BM25Okapi

class HybridRetriever:
    def hybrid_search(self, query: str, top_k: int = 10) -> list[dict]:
        # 1. 向量检索
        vector_results = self.collection.query(query_embeddings=[...], n_results=top_k)
        # 2. BM25 检索
        bm25_scores = self.bm25.get_scores(self._tokenize(query))
        # 3. RRF (Reciprocal Rank Fusion) 融合
        rrf_scores = {}
        k = 60 
        for rank, doc_id in enumerate(vector_results["ids"][0]):
            rrf_scores[doc_id] = rrf_scores.get(doc_id, 0) + 1 / (k + rank + 1)
        # ... 排序返回

3.3 结果重排
使用 Cross-Encoder 模型（如 BAAI/bge-reranker-base）进行重排，并在最终生成时附带引用来源。

工具调用与多系统 API 集成（步骤3）

4.1 工具层统一抽象
通过 Pydantic 定义严格的输入/输出 Schema，使 LLM 能够准确理解工具用途。
python
from pydantic import BaseModel, Field
from langchain_core.tools import tool

class InventoryQueryInput(BaseModel):
    product_code: str = Field(description="产品编码")

@tool(args_schema=InventoryQueryInput)
async def query_inventory(product_code: str) -> dict:
    """查询ERP系统中的库存信息"""
    async with ERPClient() as client:
        return await client.get_inventory(product_code)

4.2 统一消息模型与通道接入
解决多平台消息格式差异，将钉钉、企微、OA 等平台消息抽象为标准化格式。
python
class UnifiedMessage(BaseModel):
    platform: str       # dingtalk | wecom | oa | erp
    user_id: str
    session_id: str
    content: str
    content_type: str = "text" 
注：企微接入使用 wecom-aibot-python-sdk 基于 WebSocket 长连接通道；支持 MCP 协议的系统通过 MultiServerMCPClient 进行标准化工具发现。

容错、重试、降级与输出校验（步骤4）

5.1 错误分类与处理策略
错误类型   处理策略   实现方式
瞬态错误（网络/限流）   指数退避重试   ModelRetryMiddleware

LLM可恢复错误   转为 ToolMessage 反馈给模型   ToolErrorMiddleware

Provider 宕机   自动降级到备用模型   ModelFallbackMiddleware

过度调用（死循环）   限制单次运行调用次数   ModelCallLimitMiddleware

5.2 重试与降级中间件
python
agent = create_agent(
    model="openai:gpt-4o", tools=tools,
    middleware=[
        ModelRetryMiddleware(max_retries=3, backoff_factor=2.0),
        ModelFallbackMiddleware(fallback_models=["qwen-max", "deepseek-chat"]),
        ModelCallLimitMiddleware(max_calls=10), # 防止死循环
    ]
)

5.3 熔断器设计
连续失败达到阈值（如5次）后自动熔断（Open），经过恢复超时时间后进入半开状态（Half-Open）允许试探请求。

Docker/K8s 容器化部署（步骤5）

6.1 项目目录结构
text
enterprise-agent/
├── src/
│   ├── agent/         # LangGraph 工作流与节点
│   ├── memory/        # 短期(Redis)与长期(MySQL)记忆
│   ├── tools/         # ERP/CRM/钉钉/企微工具封装
│   ├── rag/           # 索引、混合检索、重排
│   ├── channels/      # 接入通道抽象与实现
│   ├── middleware/    # 重试、熔断、校验
│   └── api/           # FastAPI 入口与路由
├── tests/             # 单元测试与压测脚本
├── deploy/
│   ├── docker-compose.yml
│   ├── Dockerfile
│   └── helm/          # K8s Helm Chart
└── pyproject.toml

6.2 Docker Compose（本地多服务编排）
yaml
version: "3.9"
services:
  agent-api:
    build: .
    ports: ["8080:8080"]
    environment:
      REDIS_URL=redis://redis:6379/0
      DATABASE_URL=postgresql+asyncpg://agent:agent@postgres:5432/agent
    depends_on:
      redis: { condition: service_healthy }
      postgres: { condition: service_healthy }

  redis:
    image: redis:7-alpine
    healthcheck: { test: ["CMD", "redis-cli", "ping"] }

  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: agent
      POSTGRES_PASSWORD: agent
      POSTGRES_DB: agent

6.3 K8s Helm 部署
生产环境通过 Helm 部署，配置 HPA 自动扩缩容（CPU 阈值 70%），并集成 Liveness/Readiness 探针。
bash
helm install enterprise-agent ./deploy/helm

FastAPI 服务与 API 文档（步骤6）

7.1 API 路由设计
提供同步对话、SSE 流式对话以及健康检查接口。
python
from fastapi import FastAPI
from sse_starlette.sse import EventSourceResponse

app = FastAPI(title="Enterprise Agent API", version="1.0.0")

@app.post("/v1/chat")
async def chat(request: ChatRequest):
    """同步对话接口"""
    result = await agent_workflow.ainvoke({...})
    return ChatResponse(answer=result["final_output"], ...)

@app.post("/v1/chat/stream")
async def chat_stream(request: ChatRequest):
    """SSE 流式对话接口"""
    async def event_generator():
        async for event in agent_workflow.astream({...}):
            yield {"event": "message", "data": json.dumps(event)}
    return EventSourceResponse(event_generator())
启动后访问 http://localhost:8080/docs 获取自动生成的 Swagger UI 交互式文档。

7.2 压测方案
使用 Locust 进行负载测试，重点关注：
TTFT（首 Token 时间，流式场景）
P95/P99 端到端延迟
吞吐量（QPS）与错误率

建议：先在 Mock LLM 环境中测出基础设施吞吐量，再叠加真实 LLM 延迟进行全链路压测。

快速启动指南

bash
克隆项目并安装依赖
cd enterprise-agent
pip install uv && uv sync

配置环境变量
cp .env.example .env
编辑 .env 填入 LLM API Key、企业系统凭证 (ERP/CRM/钉钉/企微)

启动依赖服务（Redis + PostgreSQL）
docker compose up -d redis postgres

初始化 RAG 知识库
uv run python -m src.rag.indexer --docs ./docs/

启动 Agent 服务
uv run uvicorn src.api.main:app --reload --port 8080

访问 API 文档
open http://localhost:8080/docs

---

## 附录 A：API 调用示例

### A.1 同步问答 /v1/chat

```bash
curl -X POST http://localhost:8080/v1/chat \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <api_key>" \
  -d '{
    "session_id": "sess_001",
    "user_id": "u_1001",
    "message": "本周待发货订单有多少？",
    "channel": "wecom",
    "stream": false
  }'
```

响应示例（统一 UnifiedMessage 结构）：

```json
{
  "session_id": "sess_001",
  "reply": "本周共 1,286 笔待发货订单，其中华南仓 412 笔、华东仓 388 笔…",
  "trace_id": "trc_9f2c",
  "citations": [
    {"source": "dws_order_daily", "chunk_id": 1024, "score": 0.91}
  ],
  "latency_ms": 1840
}
```

### A.2 流式问答 /v1/chat/stream（SSE）

```text
data: {"delta": "本周"}
data: {"delta": "共 1,286 笔"}
data: {"delta": "待发货订单"}
data: [DONE]
```

### A.3 工具调用链路（多步）

```text
用户：查一下库存低于安全线的 SKU，并生成补货建议
链路：Router(intent=inventory) → Planner(2步) → Tool1:query_stock(低库存) → Tool2:gen_replenish(SKU清单) → Reviewer(校验补货量) → Reply
```

---

## 附录 B：安全加固清单

| 层级 | 措施 |
| --- | --- |
| 接入层 | Channel Token 校验、IP 白名单、消息体验签、防重放 |
| API 层 | API Key + JWT 双通道、角色权限（RBAC）、请求限流 |
| 工具层 | MCP 服务注册白名单、敏感工具二次确认、参数 Schema 校验 |
| 记忆层 | 长期记忆敏感字段脱敏、访问审计、TTL 清理 |
| 知识层 | 文档权限映射、检索结果按权限过滤、来源水印 |
| 部署层 | Secret 加密存储、网络策略最小化、镜像签名、依赖漏洞扫描（trivy） |

---

## 附录 C：可观测性接入

- **日志**：结构化 JSON 日志，全链路 trace_id 贯穿网关→API→编排→工具→LLM；保留 30 天，审计日志 180 天。
- **指标**：Prometheus 指标（QPS、延迟分位、错误率、LLM 调用次数/Token、工具成功率、记忆读写量），Grafana 大盘。
- **追踪**：OpenTelemetry + Jaeger，按 trace_id 回放一次会话全链路（Router/Planner/Tool/Reviewer 各阶段耗时）。
- **告警**：P0（服务不可用 10 分钟响应）、P1（核心功能受损 30 分钟）、P2（一般异常当日处理）；LLM 供应商异常自动降级并告警。

---

## 附录 D：常见问题 FAQ

| 问题 | 解答 |
| --- | --- |
| 为什么不用纯 LangChain 而手写编排？ | 手写 LangGraph StateGraph 能精确控制 Router/Planner/Reviewer 各阶段状态机，便于结构化校验、重试与观测，避免黑盒。 |
| 长期记忆冲突怎么处理？ | 事实三元组写入前做去重与冲突检测（同实体同谓词旧值标记过期），按访问频率提升热度，按时间修剪。 |
| RAG 检索质量如何保证？ | Dense+BM25 混合检索 → RRF 融合 → Cross-Encoder 重排，chunk 500/overlap 50，语义边界切分，检索结果附引用来源。 |
| 企微/钉钉怎么接入？ | 统一 UnifiedMessage 模型，企微走 wecom-aibot-python-sdk WebSocket 长连接，钉钉走开放平台回调，均收敛到同一 Channel 抽象。 |
| 本地无 GPU 能跑吗？ | 可以。LLM 层支持 OpenAI 兼容接口，可接 Ollama 本地小模型；RAG 向量库支持 ChromaDB 轻量模式。 |

---

## 附录 E：演进路线（Roadmap）

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| V0.5 | 六层架构 + LangGraph 编排 + 双视野记忆 + RAG + Docker Compose | 已完成 |
| V1.0 | FastAPI 同步/流式接口 + 工具调用 + 容错重试熔断 | 已完成 |
| V1.1 | MCP 标准化工具接入 + K8s Helm 部署 + HPA | 已完成 |
| V1.2 | 评测集纳入 CI 回归、全链路可观测性（OTel） | 进行中 |
| V1.3 | 多智能体协作（子 Agent 编排）、工作流画布 | 规划中 |
| V2.0 | 企业版：权限中心、审计中心、知识库多租户 | 规划中 |

---

## 附录 F：现场演示清单（面试可用）

1. `docker compose up -d` 一键起服务，5 分钟内可演示。
2. Swagger UI（/docs）展示全部接口与 Schema。
3. 流式问答展示：`/v1/chat/stream` SSE 打字机效果。
4. RAG 演示：上传一份企业文档 → 提问 → 展示引用来源与重排效果。
5. 工具调用演示：连续多步（查库存→补货建议）展示 Router/Planner/Reviewer 链路。
6. 容错演示：关掉主模型 → 自动降级备用模型并返回提示；连续失败触发熔断。
7. 压测演示：Locust 起 100 并发，展示 P95 与错误率监控面板。
